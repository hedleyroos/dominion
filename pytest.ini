[pytest]
DJANGO_SETTINGS_MODULE = dominion.conf.settings
pythonpath = .
addopts = --ignore=ve
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    perf: performance / query-count regression tests (select with '-m perf')
